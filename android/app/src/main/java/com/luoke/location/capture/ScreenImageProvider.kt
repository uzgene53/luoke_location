package com.luoke.location.capture

import android.graphics.Bitmap

/**
 * Skeleton for real screen capture pipeline (MediaProjection -> ImageReader -> Bitmap)
 */
class ScreenImageProvider {

    fun getLatestFrame(): Bitmap? {
        // TODO: replace with real MediaProjection + ImageReader pipeline
        return null
    }
}
